# Production builds use the same-origin /api path; nginx on the VPS proxies it
# to the gateway tunnel. Set VITE_API_BASE only to call a gateway directly
# (requires CORS on the gateway and an https URL to avoid mixed content).
VITE_API_BASE=/api
